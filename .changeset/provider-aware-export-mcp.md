---
'nz-legislation-tool': patch
---

Add provider capability gates to CLI export and MCP runtime tools so planned Australian jurisdictions return structured unsupported-provider errors instead of falling through to New Zealand API behavior.
