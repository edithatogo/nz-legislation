---
'nz-legislation-tool': minor
---

Fix the multi-jurisdiction beta branch so provider-backed `search`, `get`, and `cite` commands remain compatible with the existing CLI output contract.

Remove committed Docusaurus build artifacts from the repository and tighten the provider health, cache TTL, and rate-limiter behavior for Australian beta support.
