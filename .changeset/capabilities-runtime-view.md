---
'nz-legislation-tool': patch
---

Add an optional runtime view to the capabilities command so JSON output can include runtime provider registry entries and the table output can show runtime kind and support status when requested.
